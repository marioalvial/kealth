package io.github.marioalvial.kealth.core

/**
 * Singleton that contains component's critical level values
 */
object CriticalLevel {
    /**
     * Constant indicating that the component's critical level is high
     */
    const val HIGH = "HIGH"
    /**
     * Constant indicating that the component's critical level is low
     */
    const val LOW = "LOW"
}